import 'package:flutter/material.dart';

class MovieOverview extends StatelessWidget {
  /// 영화요약
  const MovieOverview({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('영화요약' * 100);
  }
}
